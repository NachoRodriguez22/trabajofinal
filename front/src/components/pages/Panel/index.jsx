import React from 'react';
import { Box } from '@mui/material';
import Logout from '../../common/Logout';

export const Panel = () => {
    return (
        <Box>
            <Logout />
        </Box>
    );
};
